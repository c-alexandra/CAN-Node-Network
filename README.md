# CAN-Node-Network
A node network of CAN-communicating hardware devices implementing various sensors and data validation methods

## ESP32

conntect eps32 board to computer and check which serial port the board is using
`ls /dev/tty*` or `ls /dev/cu.*` on mac.

